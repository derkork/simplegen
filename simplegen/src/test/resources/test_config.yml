transformations:
  - template: template.j2
    nodes: nodes
    data:
      - data/data.yml

      - includes: "**/*.yml"
        excludes: "**/narf.yml"
        base_dir: data/foo

      - includes:
          - "**/*.yml"
          - "**/*.yaml"
        excludes:
          - "**/narf.yml"
          - "**/narf.yaml"
        base_dir: data/bar


  - template: template.j2
    nodes: nodes
    data: data/data.yml